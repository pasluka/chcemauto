<%= form_for([@version, @item]) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Názov prvku" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Cena" %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit "Pridať"%>
  </div>
<% end %>
